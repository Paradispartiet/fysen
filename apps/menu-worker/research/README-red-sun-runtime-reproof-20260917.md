This exact-head reproof isolates two generic HTML runtime failures found during semantic review of the Red Sun live artifact:

1. Explicit sub-floor add-on prices must terminate a trailing-card scan without becoming recovered menu output.
2. A semantic heading title must remain canonical when a following description contains conjunction words such as `with`.

The production fix must remain generic. No restaurant-specific extraction branch or lowered global price floor is permitted by this reproof.
