{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack pnpm --version && corepack pnpm install --no-frozen-lockfile",
  "ignoreCommand": "cd ../.. && if [ \"${VERCEL_GIT_COMMIT_REF:-}\" = \"main\" ]; then git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^1}\" HEAD -- apps/web/.vercel-release; else turbo query affected --base=\"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^1}\" --packages @fysen/web --exit-code; fi",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "preview-*": true,
      "*": false
    }
  },
  "github": {
    "autoJobCancelation": true
  }
}
